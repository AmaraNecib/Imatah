package com.example.imatah.presentation.viewmodel

import androidx.lifecycle.ViewModel
import com.example.imatah.domain.usecase.AddReportUseCase
import dagger.hilt.android.lifecycle.HiltViewModel
import javax.inject.Inject

@HiltViewModel
class AddReportViewModel @Inject constructor(private val addReportUseCase: AddReportUseCase): ViewModel() {

    //TODO: add the viewmodel logic to add a report

    // this is an example

}